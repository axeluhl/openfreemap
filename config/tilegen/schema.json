{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "hosts": {
      "type": "array",
      "description": "Optional SSH aliases this config may deploy to. The deploy IP is normally passed with --host [user@]host, which overrides this list; may be empty or omitted.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "ssh_user": {
      "type": "string",
      "description": "Default SSH user for deploys when --host has no user@ prefix and --user is not given. Defaults to 'ec2-user'."
    },
    "cron": {
      "type": "boolean"
    },
    "telegram_token": {
      "type": "string"
    },
    "telegram_chat_id": {
      "type": "string"
    },
    "telegram_topic_id": {
      "type": "integer"
    }
  },
  "required": ["cron"]
}
